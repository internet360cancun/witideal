import {styled}  from '@material-ui/core/styles'

const Container = styled('section')(({theme}) => ({
  marginTop: '75px',
  padding: '1px', 
  background: theme.wd_bgColor
}))

export default Container